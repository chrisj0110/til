# asciinema cli to record gifs

`brew install asciinema`
`brew install agg`

`asciinema rec demo.cast`
# do stuff
`exit`
`agg demo.cast demo.gif`
# can then edit start and end of demo.gif in `preview`
